const INITIAL_STATE = {
  name: '',
  assertions: 0,
  score: 0,
  gravatarEmail: '',
};

export const login = (state = INITIAL_STATE, _action) => {
  switch (_action.type) {
  default:
    return state;
  }
};
